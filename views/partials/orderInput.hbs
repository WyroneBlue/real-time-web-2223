<label for="{{ id }}">
    {{ title }}: €{{ price }}
    <input
        type="number"
        name="{{ name }}"
        id="{{ id }}"
        value="{{ value }}"
        min="1"
    />

    <button> Add {{ title }}</button>
</label>